from .models import Customer, Car, Rental, Base

#this file makes the model directory a python package
#it exports our database mode so they can be imported from other files
#imports all model classes and the base class from models.py